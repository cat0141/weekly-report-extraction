---
name: weekly-report-extraction
description: 从钉钉日志抓取指定人员周报、保存本地、提取产品线信息并生成汇总文档。覆盖完整周处理流水线。
version: v4.0
created: 2026-05-03
updated: 2026-05-18
---

# weekly-report-extraction - 周报全流程处理

## 📋 触发条件

当用户要求以下任意一种操作时触发：
- **抓取周报**：要求从钉钉日志搜索/拉取指定人员本周周报
- **提取周报信息**：提供周报要求提取易云簿/财云等业务信息
- **汇总周报**：要求按产品线汇总本周进度

---

## 🎯 核心流程

### 第一阶段：从钉钉日志抓取周报（用户未提供原始周报时）

#### Step 1：搜索周报列表（通过钉钉日志 MCP）

**⚠️ 关键参数规则（2026-05-18 实测确认）：**

使用 `get_received_report_list` 拉取收到的日志：
- **必填参数**：`startTime` + `endTime` + `size` + `cursor` 四个参数必须同时传，缺任何一个报 `40035 不合法的参数`
- **size 上限 = 10**：传 20 或 100 会返回 0 条！每次最多 10 条
- **cursor 翻页**：从 0 开始，每次 +1（数字增量，不是上次返回的 cursor 值）
- **时间戳格式**：毫秒级 Unix 时间戳（如 `1776000000000`）

**拉取策略**：
```
1. 调用 get_received_report_list（分页翻页，size=10, cursor 从 0 递增）
2. 遍历 report_list 按 creator_user_name 匹配目标人员
3. 匹配到后提取 userId 和 report_id
4. 如需更多该人员历史周报，扩大时间范围继续翻页
```

**⚠️ 不要依赖 get_send_report_list 找人**：该工具只返回"当前用户自己发送的"日志，无法获取其他人列表。查找目标人员唯一可靠方式是拉取收到的日志并按人名过滤。

**没有按名称搜索的直接 API**：钉钉日志 MCP 不提供 `search_by_name` 接口。
- 替代方案：拉取所有收到的日志 → 遍历结果按 `creator_user_name` 过滤
- 或用 `get_send_report_list` 的 `report_template_name` 参数按模板名过滤

**已确认的人员信息（从 references/config.json 读取，不要硬编码）：**
- 王顺泽: userId `448599304661409792`, 模板「产品部周报」✅
- 汪玉瑶: userId `364863546608361472`, 模板「中小微事业群-小微团队汇总周报」✅
- 徐航: userId 已通过时间范围搜索获取, 模板「总部客成中心周报」✅ (reportId: 19e2fce8d3a22e15c9f74cf4d26b1080)
- 葛玉: userId 已通过时间范围搜索获取, 模板「葛玉的周报模板」✅ (reportId: 19e308fee06030f704304e042d7a0f70)

**⚠️ 重要发现（2026-05-18）**：徐航和葛玉的周报**可以通过时间范围搜索找到**，即使他们不是单独发给大王的。
- 只要大王的钉钉日志账号能**看到**这些周报（比如发到群里或抄送了），`get_received_report_list` 按时间范围翻页就能搜到
- 搜索策略：扩大时间范围（如 2026-05-01 至 2026-05-18），遍历所有页面，按 `creator_user_name` 匹配目标人名
- 找到 report_id 后直接用 `get_report_entry_details` 拉取详情，不需要 userId 也能拉

#### Step 2：获取周报详情
对匹配到的 `report_id`，调用 `get_report_entry_details` 获取完整内容。
- **优先使用 `contentV2` 字段**：每项有 `key`（字段名）、`value`（明文内容）、`type`（字段类型）、`images`（附件列表）
- **`value` 是明文文本**（含 Markdown 格式表格），不是加密值
- 备用 `report_content`：其中 `richTextValue` 是加密格式，但 `value` 也是明文，可直接使用

#### Step 3：保存原始周报
将提取的周报内容保存为本地 HTML 文件（非 Markdown，因为钉钉日志包含图片需渲染）。
- **路径**：`/root/workspace-openclaw/workspace/周报与复盘/原始周报/`
- **命名**：`人名_YYYYMMDD.html`（日期为周报提交日期）
- **格式**：使用 `references/weekly-report-template.html` 模板渲染，自动清理 `<span style="...">` 标签

### 第二阶段：按产品线提取信息（用户要求汇总时）

#### Step 4：先查差异化说明文档
**在提取之前，必须先查阅产品差异化说明文档，明确过滤规则。**

```
读取：/root/workspace-openclaw/项目管理/易云簿和财云差异化说明.md
```

这是避免产品线混淆的关键步骤，**绝不能跳过**。

#### Step 5：按产品线归类信息
从周报中逐条提取，判断归属：
- 终端企业客户 → 易云簿
- 代账公司/财务机构 → 财云
- 出现"AI会计师"、"专业版" → 财云
- 不确定时 → 标注"待确认"，不要猜测

#### Step 6：写入对应汇总文档
更新对应产品线的周报汇总文档：
- 易云簿：`/root/workspace-openclaw/workspace/周报与复盘/业务汇总/易云簿周报汇总.md`
- 财云：`/root/workspace-openclaw/workspace/周报与复盘/业务汇总/财云周报汇总.md`

在文件开头（`## 提取规则` 之前）追加新的 `## YYYY-MM-DD 本周` 区块。

#### Step 7：部署到 Nginx 静态目录 + 生成访问链接

**⚠️ HTML 在线预览方案（2026-05-18 确认）**：
生成的 HTML 文件**不能**通过文档系统 API 链接访问（返回 `Content-Type: application/octet-stream` 会触发下载）。
正确做法是复制到 Nginx 静态目录，通过 Nginx 直链预览：

```bash
# 1. 复制 HTML 到静态目录
cp /root/workspace-openclaw/workspace/周报与复盘/原始周报/人名_YYYYMMDD.html /var/www/yiyunbu-crm/pages/
cp /root/workspace-openclaw/workspace/周报与复盘/业务汇总/易云簿周报汇总_YYYYMMDD_完整版.html /var/www/yiyunbu-crm/pages/

# 2. 设置权限
chown nginx:nginx /var/www/yiyunbu-crm/pages/文件名.html

# 3. 返回 Nginx 直链（支持浏览器在线预览）
http://120.26.127.160/crm/pages/文件名.html
```

Nginx 配置：`/var/www/yiyunbu-crm/pages/` 路径映射到 `/crm/pages/`，返回 `Content-Type: text/html`，浏览器直接渲染预览，不会触发下载。

---

## ⚠️ 常见陷阱

1. **不要凭感觉归类**：看到客户名字不要自动归到当前讨论的产品线，必须查差异化文档
2. **不要遗漏产品线**：周报可能同时涉及多个产品线，全部提取不要漏
3. **AI会计师≠易云簿**：AI会计师是财云升级版，不是易云簿功能
4. **专业版≠易云簿版本**：专业版是财云的版本体系
5. **四川一新泽是财云客户**：不是易云簿客户
6. **独立标题下的内容不要默认归属**：如"售前技术支持"作为独立一级标题（不在"易云簿项目"下面），未明确标注产品线时，必须标注"待确认"，不能默认归到当前产品线
7. **合计数据无法拆分时不提取**：如"本周回款 2.41 万"同时包含两条产品线，无法拆分则不放入单产品线文档
8. **钉钉日志分页陷阱**：`get_received_report_list` `size` 最大只能传 **10**，传 20/50/100 都返回 0 条！必须用 `size=10` + `cursor` 从 0 开始每次 +1 翻页
9. **周报详情字段选择**：优先使用 `contentV2`（含格式化文本），不要直接解析 `report_content` 中的 `richTextValue`（加密格式）
10. **HTML 标签清理（8步转换流水线）**：`contentV2.value` 是 Markdown+HTML 混合内容，必须按以下顺序转换：
    - ① 移除 `<span>` 标签（保留内容）
    - ② `![alt](url)` → `<img src="url">`（用占位符暂存，避免后续处理干扰）
    - ③ `|...|...|` 表格 → `<table>` 解析（跳过分隔行 `|---|---|`）
    - ④ 恢复图片占位符
    - ⑤ `## 标题` → `<h2>`，`### 标题` → `<h3>`
    - ⑥ `**粗体**` → `<strong>`
    - ⑦ `---` 分割线 → `<hr>`
    - ⑧ 按 `\n\n` 分段，每段转 `<p>`，已有 HTML 标签的段直接保留
11. **钉钉日志 MCP key 存储**：大王提供的完整 key 保存在 `references/config.json` 的 `mcp.key` 字段（2026-05-18 确认有效）。如调用返回 `PARAM_ERROR`，检查 key 是否正确，不要使用 config.yaml 中可能被截断的 `***`
12. **原始周报 HTML 格式**：原始周报保存为 `.html` 而非 `.md`，文件名格式 `人名_YYYYMMDD.html`，因为钉钉日志中包含图片，HTML 可渲染图片而 Markdown 不能。使用 `references/weekly-report-template.html` 模板渲染
13. **汇总文件名含日期**：业务汇总文件命名加日期后缀（如 `易云簿周报汇总_20260518.html`），避免覆盖历史汇总
14. **HTML 模板 + JSON 分离**：所有 HTML 文件使用模板+数据分离方案，模板在 `references/*.html`，JSON Schema 在 `references/*-schema.json`，不要每次都手写 HTML
15. **钉钉日志 MCP 完整指南**：详见 `references/钉钉日志MCP使用指南.md`，包含 7 个工具的完整参数说明、示例代码、错误码对照表
16. **HTML 在线预览（2026-05-18）**：生成的 HTML 文件必须部署到 `/var/www/yiyunbu-crm/pages/` 目录，通过 Nginx 直链访问（`http://120.26.127.160/crm/pages/文件名.html`）。**不能**通过文档系统 API 链接访问，因为返回 `application/octet-stream` 会触发浏览器下载而不是预览
17. **时间范围搜索策略**：即使目标人员不是单独发送周报给大王（比如发到群里），只要大王能看到这些日志，通过扩大 `get_received_report_list` 的时间范围（如 5 月 1 日至 5 月 18 日）并翻页遍历，按 `creator_user_name` 过滤即可找到 report_id。找到后直接用 `get_report_entry_details` 拉取详情

---

## 📁 文档体系

| 文档 | 路径 | 用途 |
|------|------|------|
| 原始周报 | `原始周报/人名_YYYYMMDD.html` | 个人原始周报（HTML 格式，支持图片渲染） |
| 易云簿周报汇总 | `业务汇总/易云簿周报汇总_YYYYMMDD.html` | 易云簿信息汇总（含日期，HTML 格式） |
| 财云周报汇总 | `业务汇总/财云周报汇总_YYYYMMDD.html` | 财云信息汇总（含日期，HTML 格式） |
| **对比汇总** | `业务汇总/易云簿周报对比汇总_YYYYMMDD.html` | 上周 vs 本周数据对比 + 任务完成对比 + 冲突项 |
| **运营总监视角汇总** | `业务汇总/易云簿运营周报_YYYYMMDD_总监视角.html` | 运营总监视角深度分析（含洞察/风险/行动建议） |
| 差异化说明 | `/root/workspace-openclaw/项目管理/易云簿和财云差异化说明.md` | 产品线区分规则（提取前必读） |
| 配置文件 | `周报与复盘-skill/references/config.json` | 人员/路径/MCP/模板集中配置 |
| HTML 模板 | `周报与复盘-skill/references/*.html` | 原始周报 + 业务汇总渲染模板 |
| JSON Schema | `周报与复盘-skill/references/*-schema.json` | 数据结构示例，填充模板用 |
| 钉钉日志 MCP 指南 | `周报与复盘-skill/references/钉钉日志MCP使用指南.md` | 7个工具的完整参数/示例/踩坑记录 |
| **运营总监提示词** | `周报与复盘-skill/references/运营总监分析提示词.md` | 运营总监分析角色设定、目标、维度、输出规范 |

---

## 🔧 配置文件驱动模式（v3.0 新增）

所有可变信息集中在 `周报与复盘-skill/references/config.json`，包括：

### 人员配置（支持增删/开关）
```json
"team_members": [
  {
    "name": "徐航",
    "department": "小微营销组",
    "focus": "前端营收、新签单量、线索转化情况",
    "dingtalk_user_id": "真实userId",  // 替换为真实值
    "enabled": true                     // false = 临时跳过此人
  }
]
```
- **新增人员**：数组中追加对象
- **临时停用**：设 `enabled: false`，无需删除

### 路径与模板配置
```json
"paths": {
  "raw_reports_dir": "/root/workspace-openclaw/workspace/周报与复盘/原始周报",
  "summary_dir": "/root/workspace-openclaw/workspace/周报与复盘/业务汇总",
  "diff_rules_file": "/root/workspace-openclaw/项目管理/易云簿和财云差异化说明.md"
},
"templates": {
  "weekly_report": ".../weekly-report-template.html",
  "summary": ".../summary-template.html"
}
```

### HTML 模板 + JSON 数据分离
采用模板（定义样式）+ JSON 数据（填充内容）方案：
- `weekly-report-template.html` — 原始周报渲染模板（Mustache 风格 `{{占位符}}`）
- `summary-template.html` — 业务汇总渲染模板（含指标卡片、数据表格、关键发现）
- `weekly-report-data-schema.json` — 原始周报 JSON 数据结构示例
- `summary-data-schema.json` — 业务汇总 JSON 数据结构示例

**好处**：样式一次定义，数据随时替换，新增人员无需改模板。

---

## 🔧 周报 v1.1 升级规范（大王 2026-05-11 要求）

当大王要求"升级周报"、"周报不能是流水账"、"写周报 vX.X 版本"时，**必须遵循以下规范**：

### 核心原则
1. **禁止纯流水账** — 不只是罗列数据，每条数据后必须带分析/判断/建议
2. **以目标为导向** — 开头放"本月目标 vs 当前进度"对比表，所有数据围绕"差多少"展开
3. **必须有上周工作总结** — 放在文档第一部分，总结做了什么、进展到哪一步
4. **必须写"数据缺失清单"** — 列出各渠道/各人下周必须填报的具体项（打勾框格式），明确标注谁缺什么，否则无法支撑业务判断

### 文档结构（v1.1 标准模板）
```
一、上周工作总结
  - 回款与销售
  - 线索获取
  - 数据口径统一进展
  - 其他重要事项（内容营销、产品迭代等）

二、数据 vs 月度目标
  - 目标进度总览（目标、累计、进度、差距）
  - 五渠道漏斗（按渠道拆分，标注"待补充"项）

三、数据缺失清单（下次必填）
  - 按人/按渠道列出缺失项，用 [ ] 打勾框
  - 示例：
    ### 葛玉（渠道1 付费 + 渠道2 免费）
    - [ ] 投流视频"主页始终可见"是否已执行？
    - [ ] 免费渠道本周线索量、有效线索量各多少？

四、业务分析
  - 每个关键数据段带分析判断，指出风险和机会

五、本周行动项
  - 表格格式：任务 | 负责人 | 优先级 | 说明

六、下周关注
```

### 重要规则
- **保存先写服务器本地，不直接建飞书/钉钉文档** — 大王确认后再发
- **用 `doc-link-by-path` skill 获取文档链接** — 手机可直接访问
- **不要发消息到群里收集信息** — 如果数据不全，直接在文档里写"还缺什么"，作为下次数据收集的规范
- **格式适配手机** — 飞书沟通优先列表/纯文本，避免大表格

---

## 💬 汇总输出格式（写入业务汇总文件）

在汇总文件中追加 `## YYYY-MM-DD 本周（MMDD-MMDD）` 区块，包含：

```markdown
### 💰 业绩
- 徐航：新签 X 单（Y 易云簿 + Z 财云），回款 X 万
- 汪玉瑶（运营组）：易云簿收款 X 元
- 葛玉（投流）：投流花费 X 元，新增线索 X 条（有效 X 条）

### 📈 投流与线索数据
- 有效线索目标：X，完成 X%
- ROI 目标值：X，完成 X

### 💡 产品需求（一线反馈）
- 需求1
- 需求2

### 📋 运营
- 运营事项1
- 运营事项2

### 🧠 关键思考
- 思考1

### 📅 下周计划
- 徐航：...
- 葛玉：...
- 王顺泽：...
```

---

## 📊 对比汇总生成规范（v4.0 新增）

当大王要求"对比周报"、"周报汇总要有对比"时，生成**对比汇总 HTML**，必须包含以下模块：

### 必须包含的模块
1. **人员提交状态表**：上周 vs 本周谁交了谁没交
2. **核心指标周环比对比表**：上周值 | 本周值 | 变化趋势（↑/↓/—/⚠️）| 说明
3. **任务完成对比**：上周计划 → 本周完成状态（✅ 已完成 / ⏳ 待完成 / 🆕 新任务）
4. **不确定与冲突项**：单独模块，标注数据异常、内容重复、缺失人员
5. **关键思考与洞察**：从周报中提取的业务思考
6. **下周计划汇总**：按人列出

### 趋势标识
- `↑ +X (Y%)` — 增长（绿色 `#52c41a`），越高越好
- `↓ -X (Y%)` — 下降（红色 `#ff4d4f`），需关注
- `— 持平` — 无变化（灰色）
- `⚠️ 数据缺失` — 上周或本周未报告（黄色）

### 数据标注规则
- **指标类型区分**：明确标注"本月累计"还是"本周新增"，不可混淆
- **原文引用**：所有结论必须附原始周报引用，使用 `<details>` 折叠组件：
```html
<details class="quote">
    <summary>📄 原文引用 <a href="原文链接" target="_blank">查看完整周报 ↗</a></summary>
    <div class="quote-content">原始周报内容...</div>
</details>
```
- **原文链接**：每个引用带 `target="_blank"` 新窗口打开链接

### 仅单周有数据的处理
- 不强行对比，标注"仅本周有数据"
- 列出本周工作内容即可
- 在冲突项中标注"无法进行周环比对比"

---

## 🧠 运营总监视角分析（v4.0 新增）

当大王要求"用运营总监视角分析"或"深度分析周报"时：

### 角色设定
首席运营总监，10年 SaaS 运营经验，深谙财税 SaaS 行业、小微企业老板心理、线索到营收全链路。

### 核心目标
- **短期**：获取更多有效线索量，降低 CPL，提升转化率
- **长期**：驱动营收持续增长，提升 LTV，构建增长飞轮

### 分析维度
1. **线索漏斗分析**：各渠道数量、有效率、转化断点、CPL 健康度
2. **营收健康度**：新签 vs 续费、客单价趋势、回款完成率
3. **PMF 匹配度**：一线反馈痛点是否影响转化？竞品威胁？
4. **营销动作有效性**：投流/内容/渠道的 ROI，哪些可规模化？
5. **团队协同**：跨部门协作是否顺畅？有无责任推诿？

### 输出结构
1. **核心洞察（Insights）**：3-5 条直击本质的判断，拒绝表面描述
2. **风险预警（Risks）**：数据异常、目标偏离、流程卡点 + 根因假设
3. **缺失数据清单**：明确指出缺少什么数据、需补充什么才能完整分析
4. **行动建议（Actions）**：具体、可落地、带责任人/时间节点（P0/P1/P2 分级）
5. **数据引用（Quotes）**：对比分析时必须引用**两周原文**（上周+本周），使用 `<details>` 折叠组件，支持新窗口打开原文链接
6. **运营总监点评**：针对每个问题/现象给出具体指导建议

### 原文引用规则
- **数据对比时必须同时引用两周原文**（上周 vs 本周），不能只引一周
- 使用 `<details class="quote">` 折叠组件，默认展开
- 每个引用带 `target="_blank"` 原文链接，支持新窗口打开
- 原文用 `**[上周 YYYY-MM-DD]**` / `**[本周 YYYY-MM-DD]**` 标注时间

### 缺失数据标注
- 对信息不全的模块，用 `missing-data` 样式框标注
- 明确说明：缺什么数据 → 有什么影响 → 需要补充什么才能完整
- 运营总监针对缺失数据给出补充建议

### 参考文件
详见 `references/运营总监分析提示词.md`
